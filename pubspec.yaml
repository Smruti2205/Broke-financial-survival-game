name: broke_game
description: A financial survival game
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.15.2
  firebase_auth: ^5.7.0
  cloud_firestore: ^5.5.0
  google_sign_in: ^6.2.1
  # Firebase (optional - remove if not configured)
  # firebase_core: ^3.0.0
  # firebase_auth: ^5.0.0
  # cloud_firestore: ^5.0.0
  # firebase_database: ^11.0.0

  # State management
  provider: ^6.1.0

  # Audio
  #audioplayers: ^6.0.0

  # URL Launcher
  url_launcher: ^6.2.0

flutter:
  uses-material-design: true